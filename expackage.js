//External Modules ,external package , core modules
const colors = require("colors");

console.log(colors.red("This is red text"));
console.log(colors.green("This is green text"));
console.log(colors.blue("This is blue text"));

//An external package is code written by someone else that you can install and use in your own project instead of writing everything from scratch.
//In Node.js, these packages come from the npm (Node Package Manager) — a huge library of ready-made tools.